import ComingSoon from '@/components/ui/ComingSoon';
import { FileText } from 'lucide-react';

export default function Page() {
  return <ComingSoon title="Documents" description="Upload, summarize, and extract actions from documents and PDFs." phase="Phase 3" Icon={FileText} />;
}
